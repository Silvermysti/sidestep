# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local config (machine-specific, not for sharing)
.claude/

# Stray archive — a zip of the project should not live inside the project
project.zip

# Large demo recordings — kept locally, too big for the repo
*.mp4

# Working design art — the raw sprite/scene source, kept locally. The art the
# extension actually ships lives in source/public/ and load-unpacked/.
assets/Animation images/
assets/Bunny.png
